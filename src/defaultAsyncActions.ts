/**
 * defaultAsyncActions are the basic action names assigned to any
 * async action, on top of `start`
 */
const defaultAsyncActions = ['success','error','started','cancelled'];

export default defaultAsyncActions;